# DocLab worker dependencies.
# Tabular path (M2): pandas/scikit-learn/xgboost/datasets.
# Image path (M9): torch + torchvision (MPS on Apple Silicon, CPU fallback).
# Text path (M10): transformers + peft LoRA + rouge-score (ROUGE-L).
pandas
scikit-learn
xgboost
datasets
pytest
torch
torchvision
pyyaml
transformers
peft
rouge-score
accelerate
